# Work log - 2026-07-31

## Prospective fixed-50 Golovin extension

The researcher accepted a numerical, uncertainty-aware practical
diminishing-returns framework and explicitly rejected retaining a ratio of
successive error reductions, even as a secondary statistic.

A new experiment definition and immutable 450-row matrix were prepared:

```text
golovin_fixed50_extended_resolution_convergence_v1
```

The design contains 50 independent collision streams at each of nine
factor-of-two resolutions from 4,096 through 1,048,576 SDs.  It uses fresh
collision seeds, run labels, run paths and experiment-specific controlled
bundle labels.  No model data have been generated and the matrix manifest
still records `submission_authorized=false`.

Before submission, the new experiment's full-state output cadence was reduced
prospectively from 300 to 600 s.  Every formal decision is already registered
at 600-s intervals, so this removes unused intermediate states without
changing collision physics or an estimand.

The practical decision keeps the prospectively defined one-percentage-point
minimum worthwhile improvement, requires one-sided 95% bootstrap upper bounds
inside that margin for two consecutive doublings, and checks stability
between the 40- and 50-member prefixes.  The 250/1000-bin products remain
sensitivity diagnostics.

Added `analyze_golovin_convergence_law.py`.  It independently bootstraps
members and fits

\[
E(N)=E_\infty+a(N/N_{\min})^{-p}
\]

over the highest four, five and six levels for L1 and absolute \(M_0\)/\(M_6\)
bias.  The fit reports floor, exponent and projected next-doubling
uncertainty, but is explicitly prevented from selecting a resolution.
The standard resolution-analysis Slurm entrypoint now runs the practical and
convergence-law stages in the same audited allocation when their reviewed
YAML sections are present.

Measured fixed-10 costs imply about 55--61 physical member CPU-hours and
26--29 GB of new raw SCRATCH for the full fixed-50 ladder including the new
1,048,576-SD level.  The live `m301324` filesystem has about 15 TB free and no
current user job.  This is a capacity check, not submission authorization.

Validation after implementation:

```text
Ruff: pass
Ruff format: pass
pytest: 102 passed
Bash syntax: pass
git diff --check: pass
matrix rows: 450 unique cases
```

Next gate: publish the exact source revision, rebuild it on Levante, generate
and verify the nine fresh controlled bundles, and disclose the final model
and analysis allocations before any Slurm submission.

## Sixteen-worker execution gate and submitted chain

The researcher approved increasing the model concurrency from eight to 16
physical cores after a resource explanation.  This does not make one CLEO
box use 16 MPI ranks.  Each `collisions0d` member remains serial and
one-threaded; OpenMPI/Slurm launch 16 different members at the same time on
one shared node.  `--hint=nomultithread` asks for physical cores rather than
treating the node's second hardware thread per core as another worker.

The layout-probe wrapper was generalized from a hard-coded four-worker check
to a reviewed positive integer supplied by
`PARALLEL_PROBE_EXPECTED_WORKERS`, retaining four as the default.  Local
validation before push reported:

```text
pytest: 102 passed
Ruff: pass
Ruff format: pass
Bash syntax: pass
git diff --check: pass
```

The first 16-worker probe, job `26587062`, requested `mh0731/shared`, one
node, 16 physical tasks, one CPU per task, 14.4 GiB and two minutes.  All 16
worker steps completed, but the final parser failed on Levante's system
Python because it used the newer `list[int]` annotation syntax.  This was a
tooling compatibility failure: no CLEO model, input bundle or Zarr data was
created.  The parser was made version-neutral, covered by a regression
assertion and published as commit `8e39a60`; GitHub quality run
`30617662285` passed.

Replacement probe `26587104` completed `0:0` in 33 seconds.  Slurm reported
16 requested physical CPUs and 32 allocated logical CPUs under the
non-multithread hint.  The 16 exclusive worker steps all completed and their
start-time span was 0.290723 seconds:

```text
PARALLEL_MEMBER_LAYOUT_PROBE_PASS=1
```

The exact production preflight then confirmed:

- detached Levante project commit `8e39a60`;
- pinned CLEO commit `83318c23223546d10759d202d70f4fa2f7fe4688`;
- 450 unique matrix rows and matrix SHA-256
  `cc838bc63f2d939f99f21013836d134195de6339658ba9555ca48d17d9a293ed`;
- absent fresh build, run, record and all nine bundle targets;
- approximately 15 TB of free SCRATCH; and
- present account-local Python, CLEO, YAXT and YAC dependencies.

The disclosed chain used `mh0731/shared` and no GPU:

| Stage | Physical cores | Memory | Walltime ceiling | Maximum core-hours |
|---|---:|---:|---:|---:|
| exact build | 8 | 4 GiB | 10 min | 1.33 |
| bundle preparation | 1 initially | 940 MiB | 20 min | 0.33 |
| model | 16 | 14.4 GiB | 5 h | 80.00 |
| analysis | 1 | 940 MiB | 3 h | 3.00 |

The complete ceiling is about 84.7 physical core-hours; measured scaling
predicts about 56--63 actual physical core-hours.  With 16 concurrent
members, expected elapsed compute time after allocation is approximately
4.25--6 hours, excluding queueing.

Exact build job `26587225` passed in 2 minutes 11 seconds with an exact
project/CLEO build manifest.  Its stderr contained only the two already-known
upstream unused-variable warnings.

The first input-only bundle job, `26587226`, generated and froze the first
eight resolution bundles but ran out of its 940-MiB allocation while
constructing the 1,048,576-SD bundle.  The stage was intentionally placed
before the model: dependent jobs `26587227` and `26587228` were cancelled
without allocation, so the failure consumed no model CPU time and created no
scientific member output.  The incomplete bundle and partial ladder record
were preserved, not silently overwritten:

```text
golovin_fixed50_extended_N1048576_v1_failed_job26587226
golovin_fixed50_extended_resolution_v1_bundle_record_failed_job26587226
```

Retry `26587405` used 4 GiB and generated only the missing 1,048,576-SD
bundle.  It completed in 31 seconds with empty stderr.  A separate read-only
verification then checked all nine bundles against their expected resolution,
configuration, project commit, CLEO commit, file modes and SHA-256 inventory;
all passed.

Replacement production model job `26587532` was submitted with 16 physical
workers, 14.4 GiB and a five-hour ceiling.  Dependent analysis job `26587533`
requests one physical core, 940 MiB and three hours and will start only if the
model succeeds.  Both use email notification to
`anirudh.arora.research@gmail.com` on completion or failure.

At the documentation checkpoint, the model was running on `l40000`; 166 of
450 final `manifest.txt` records were present, 16 model steps were active and
no worker stderr file was non-empty.  The faster early progress is expected
because the matrix begins with lower resolutions; the highest-resolution
members dominate the remaining work.  No convergence conclusion exists
until job `26587533` finishes its full matrix/checksum audit and publishes the
registered analyses.
