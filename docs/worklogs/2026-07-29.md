# Work log: 2026-07-29

This record documents the controlled Golovin initializer implementation. It is
written as a learning and provenance artifact. Nothing in this log is a
Golovin convergence result or authorization for a production ensemble.

## 1. Starting point

ADR 0004 had already registered the scientific construction:

- one representative per logarithmic-volume bin;
- the same finite-support continuous DSD at every \(N_\mathrm{SD}\);
- deterministic largest-remainder integer multiplicities;
- exact rounded physical-droplet total;
- bin-integrated liquid volume preserved through representative radius;
- \(M_0/M_3\) controlled, \(M_6\) checked but not forced;
- one binary frozen within resolution across collision streams.

The configuration still reported `specified_not_implemented`. The existing
Clara-derived operational initializer was retained and remained the default.

## 2. Implementation boundary

The new scientific code is project-owned:

```text
scripts/controlled_initialization.py
```

It intentionally contains no CLEO import. This lets the numerical construction
be tested locally without compiling CLEO.

The existing input entry point now provides the CLEO integration:

```text
scripts/prepare_collisions0d_inputs.py
```

It wraps the deterministic population in an object implementing CLEO's
attribute-generator methods. That object is passed to CLEO's native
`generate_initial_superdroplet_conditions` function. The upstream CLEO source
and binary format were not edited.

## 3. Numerical issue 1: an exact total near \(10^{19}\)

The configured concentration and \(10^{12}\,\mathrm{m^3}\) box imply:

```text
8388608000000000000 physical droplets
```

Float64 cannot distinguish every adjacent integer at this magnitude. The
first implementation multiplied float probabilities by the total, took floors
and summed them. It could lose hundreds of droplets because several different
large integers map to the same float.

The corrected implementation:

1. converts probabilities into high-resolution integer weights;
2. uses Python integers for quota floors and remainders;
3. gives residual droplets to the largest remainders;
4. breaks ties by bin index;
5. verifies the exact Python-integer total.

The final \(M_0\) total is therefore exact, not merely close within a relative
tolerance.

## 4. Numerical issue 2: nearly equal exponential values

With many narrow bins, directly calculating

```text
exp(-lower / scale) - exp(-upper / scale)
```

subtracts nearly equal numbers. At \(N_\mathrm{SD}=32768\), this could move a
calculated representative just outside its source bin.

The corrected bin integrals use:

- `expm1` for stable exponential differences;
- conditional first and second moments within a bin;
- series expansions for narrow dimensionless bin widths.

Float64 is used explicitly because NumPy `longdouble` has platform-dependent
precision. The scientific-array hash should not depend on whether the code ran
on macOS or Levante merely because one platform gives `longdouble` more bits.

The numerical sweep then passed for:

```text
64, 128, 256, 512, 1024, 4096, 16384, 32768 SDs
```

The 16-SD case was deliberately rejected because its \(M_6\) error was about
4.55%, above the registered 1% gate.

## 5. CLEO integration

`prepare_collisions0d_inputs.py` now declares two distinct families.

### `operational_stochastic`

- retains the PerformanceTestingCLEO-compatible sampled radii;
- retains random coordinates;
- accepts and reports a NumPy seed;
- remains the default so prior runs do not silently change meaning.

### `controlled`

- requires `--controlled-config`;
- rejects `--seed` because the construction is deterministic;
- creates deterministic log-volume representatives and multiplicities;
- uses CLEO's deterministic coordinate generator;
- calls CLEO's native grid and superdroplet writers;
- creates a new JSON audit;
- refuses to overwrite inputs or the audit.

The production matrix runner has **not** been switched to the controlled
family. One immutable per-resolution bundle and its checksum must first be
implemented so every collision member reuses the same binary rather than
regenerating it.

## 6. Current 4096-SD population

The local scientific constructor produced:

| Quantity | Result |
| --- | ---: |
| physical-droplet total | `8388608000000000000` exactly |
| represented \(M_0\) | `8388608 m-3` |
| relative \(M_0\) error | `0` |
| relative \(M_3\) error | `0` at printed precision |
| relative \(M_6\) error | `-8.333e-7` |
| liquid-water content | `0.99823635 g m-3` |
| representative radius range | `1.00053–74.96021 μm` |
| population SHA-256 | `c2d02c94d7d55edda95ebf1fdd002a46c6fdb83042c7d47e9ce227f080551205` |

This hash identifies the project scientific arrays—multiplicities and
radii—not yet a CLEO native binary.

## 7. Tests

Ten new local tests passed. Seven exercise the scientific constructor and
adapter:

1. exact largest-remainder total and deterministic tie behavior;
2. \(M_0/M_3/M_6\), minimum multiplicity and source-bin gates;
3. repeatable arrays and population hash;
4. deliberate M6 rejection at 16 SDs;
5. deliberate impossible-minimum-multiplicity rejection;
6. CLEO-compatible NumPy dtypes and deterministic coordinates;
7. JSON audit hashes and overwrite refusal.

Three additional entry-point tests verify that:

1. controlled initialization rejects a random seed;
2. operational initialization rejects controlled-only CLI options;
3. the controlled one-box path rejects a multi-cell boundary definition.

The current status string is:

```text
implemented_local_unit_validated_native_binary_pending
```

That wording is intentionally narrower than “validated”.

## 8. Documentation changed

The following were updated:

- README usage and validation boundary;
- ADR 0004 implementation status and next gates;
- Golovin convergence protocol remaining gates;
- Stage-0 guide statements that had become stale;
- repository contract test;
- a new first-principles
  `docs/implementation/controlled-initialization-guide.md`.

## 9. Compute

No Levante allocation, Slurm submission or model run was used for this work.
All calculations were inexpensive local Python tests.

## 10. Next gate

Before any Levante action, the complete compute request must be reported to
Anirudh. The proposed first pilot is:

- account `bb1153`;
- `shared` partition;
- 1 node;
- 1 task;
- 1 CPU;
- 940 MiB;
- 10 minutes;
- one 4096-SD native binary generation/readback;
- no ensemble.

Only after that input gate passes should a single short compiled Golovin run be
considered. Production convergence compute remains blocked by frozen-bundle
reuse, diagnostic grid robustness, compute/storage measurement and review.

## 11. Controlled native-binary gate

A permanent input-only gate was added:

```text
scripts/validate_controlled_initialization_binary.py
scripts/levante/validate_controlled_initialization.sbatch
```

The Python validator reads CLEO's dimensional attributes through
`cleopy.initsuperdropsbinary_src.read_initsuperdrops`. It checks:

- native binary SHA-256 against the creation audit;
- 4096 complete and equal-length attributes;
- one-gridbox membership;
- finite positive wet radii and multiplicities;
- non-negative solute masses;
- coordinate bounds;
- the exact Python-integer physical-droplet total;
- read-back \(M_0,M_3,M_6\) against the initializer audit;
- absence of collision-model Zarr output.

The job was submitted only after the resource disclosure:

| Item | Requested |
| --- | --- |
| account / partition | `bb1153` / `shared` |
| nodes / tasks | 1 / 1 |
| CPUs per task | 1 |
| memory | 940 MiB |
| walltime | 10 min |
| execution | serial CPU, no GPU |
| scope | one 4096-SD input bundle; no model; no ensemble |

Job `26534015` completed with exit code `0:0` in 11 seconds. Slurm allocated
two CPUs even though one was requested; the code still used one thread.
Batch `MaxRSS` was 3916 KiB and stderr was empty.

The input and native-reader checks passed:

```text
CONTROLLED_INITIALIZATION_NATIVE_READBACK_PASS=1
CONTROLLED_INITIALIZATION_NATIVE_GATE_PASS=1
represented_real_droplets=8388608000000000000
M0_readback=8388608.0
M3_readback=238740390128.70804
M6_readback=1.358817751738688e16
native_superdroplet_sha256=d805fb278ed070396d8bf3bb0d655138f5f1124901d5ea917279f99e270420f2
```

No `collisions0d_solution.zarr` was created. The complete raw input-only record
is 248 KiB under:

```text
/scratch/b/b383673/SDM/CLEO-SDM-Convergence/runs/
  controlled_initialization_validation/controlled_init_n4096_v1
```

### Cross-platform hash finding

The gate also revealed a subtle but useful limitation. The macOS NumPy 1.26.4
scientific-array hash is `c2d02c94...551205`, while Levante NumPy 2.5.1
produces `11d4871d...c5d08`. Direct comparison found:

- 86 multiplicities with small integer redistributions while preserving the
  exact total;
- a maximum radius difference of `1.60e-18 m`;
- a maximum relative radius difference of `2.34e-13`;
- the same registered moment results to the checked tolerance.

The separate source-array and native-readback hashes also differ because CLEO
stores dimensionless radii and the reader re-dimensionalizes them. This is not
a physics-gate failure, but it means that “float64” alone does not promise
cross-platform byte identity. The permanent ensemble must generate each
resolution's bundle once on the pinned Levante stack and reuse that exact
native file after checksum verification.

## 12. Status after the native gate

Validated:

- scientific construction and unit gates locally;
- one CLEO-native 4096-SD write/read round trip on Levante;
- exact represented number, moment preservation and native artifact checksum.

Still pending:

- immutable one-bundle-per-resolution creation and reuse;
- independent same-stack byte-replay validation;
- fixed-bin diagnostic robustness implementation;
- one short compiled controlled Golovin member;
- any convergence ensemble or convergence conclusion.

## 13. Frozen-bundle software layer

The next non-compute gate was implemented without submitting Levante work.

New files:

```text
scripts/controlled_bundle.py
scripts/levante/prepare_controlled_bundle.sbatch
tests/test_controlled_bundle.py
```

Changed files:

```text
scripts/materialize_collisions0d_config.py
scripts/levante/common.sh
scripts/levante/run_collisions0d.sbatch
```

### Bundle creation contract

One resolution gets one named, non-overwriting directory under the persistent
HOME bundle root. Input generation and native readback use the already
validated controlled path. Finalization requires:

- passing creation and native-readback schemas;
- matching \(N_\mathrm{SD}\);
- matching grid and superdroplet SHA-256 values across audit, readback and
  actual files;
- identical represented physical-droplet totals;
- all native readback gates true;
- a runtime configuration pointing at the exact bundle files;
- an initializer-source snapshot matching the creation audit.

The manifest records:

- a normalized scientific definition;
- project and pinned CLEO commits;
- Python, NumPy, platform, host and Slurm provenance;
- source and readback population fingerprints;
- relative path, size and SHA-256 for every required artifact.

Finalization removes write bits from every bundle file. An existing path or
manifest is never overwritten. A failed preparation remains visibly
incomplete and cannot pass `verify`.

### Member reuse contract

`run_collisions0d.sbatch` now has two explicit paths:

```text
operational_stochastic
    requires INITIALIZATION_SEED
    rejects CONTROLLED_BUNDLE
    creates member-local grid and superdroplet binaries

controlled
    Golovin-only at this gate
    rejects INITIALIZATION_SEED
    requires CONTROLLED_BUNDLE
    verifies the bundle before and after CLEO
    points the runtime YAML directly to the frozen binaries
    never calls the initializer
```

The completed member manifest records the bundle path plus manifest, grid and
superdroplet SHA-256 values. A wrong resolution, CLEO pin, scientific
definition, file size, checksum or writable artifact stops execution.

The controlled matrix remains disabled. Direct single-member reuse must pass
before array support is enabled.

### Local validation

Thirty-three targeted tests pass, including:

- frozen finalization and verification;
- wrong-resolution refusal;
- changed-byte refusal;
- read-only artifact enforcement;
- scientific status metadata excluded from the physical fingerprint;
- frozen inputs referenced without copying or a member-local input directory;
- one-path-only materialization refusal;
- all previous controlled-constructor, operational-input and repository
  contracts.

Ruff, formatting, Python compilation, Bash parsing and `git diff --check`
also pass for the implementation.

### Next disclosed compute gate

No new Slurm job has been submitted. Proposed bundle-only validation:

| Item | Request |
| --- | --- |
| account / partition | `bb1153` / `shared` |
| nodes / tasks | 1 / 1 |
| CPUs per task | 1 |
| memory / walltime | 940 MiB / 10 min |
| execution | serial CPU; no GPU |
| scope | one 4096-SD bundle creation/readback/finalization |
| explicitly excluded | collision model, ensemble, convergence claim |

## 14. Persistent 4096-SD bundle creation on Levante

After the researcher approved the exact request above, the clean isolated
Levante worktree was advanced from the previous validation commit to exact
project commit:

```text
e1935d7381535b793fda8b5905145ae7a9a9e8fb
```

The fetch used the public HTTPS repository URL because the isolated worktree's
SSH remote did not have a usable GitHub identity in that login shell. The
checkout remained detached and clean.

### Submitted work

Job `26534596` requested:

| Item | Value |
| --- | --- |
| account / partition | `bb1153` / `shared` |
| nodes / tasks / CPUs per task | 1 / 1 / 1 |
| memory / walltime | 940 MiB / 10 minutes |
| execution | one serial Python/CLEO input-generation and readback process |
| resolution | 4096 superdroplets |
| excluded | collision executable, Zarr, GPU, ensemble |

It created:

```text
/home/b/b383673/SDM/CLEO-SDM-Convergence-records/controlled_bundles/
  golovin_controlled_N004096_v1
```

No pre-existing target was overwritten.

### Slurm result

```text
State:       COMPLETED
ExitCode:    0:0
Elapsed:     00:00:15
ReqCPUS:     1
AllocCPUS:   2
ReqMem:      940M
batch MaxRSS: 3.76M
stderr:      empty
```

Levante allocated two CPUs even though one was requested. The job explicitly
set `OMP_NUM_THREADS=1`; the code remained serial.

### Scientific and artifact result

The creation audit and CLEO-native readback both passed:

- \(N_\mathrm{SD}=4096\);
- represented physical-droplet total
  `8388608000000000000`;
- one grid box;
- registered M0/M3/M6 and coordinate checks;
- no collision-model output;
- no Zarr directory.

Independent verification after job completion printed:

```text
CONTROLLED_BUNDLE_VERIFY_PASS=1
```

The persistent directory contains 11 files and uses 290,319 bytes. All 11
files have every write bit removed. Important identities are:

```text
scientific definition:
59d4d68ed70fc14916976e55112d7fb3af0b29bd5d210eb6adc9bafea40a46db

native superdroplet file:
d805fb278ed070396d8bf3bb0d655138f5f1124901d5ea917279f99e270420f2

bundle manifest:
daf8c66c86df98fb0bbaf6e69bd7c832ab42e9463305e8faca7ff866ce45bdfe
```

The native superdroplet hash is the same as the earlier input-only native gate,
which is useful same-stack evidence. It is not yet the deliberately independent
regeneration/replay gate registered in the protocol.

`population_sha256_exact_match=False` is expected and is not a failed physical
gate. CLEO stores radii in dimensionless representation and re-dimensionalizes
them during readback. The ensemble will reuse the checked native file itself,
so collision members do not depend on reproducing the pre-write float arrays.

The compact record is stored in:

```text
results/controlled_initialization_bundle_n4096_v1/
```

It contains the manifest, audits, configuration snapshots, Slurm logs,
human-readable explanation and checksums, but not a duplicate of the native
binary.

## 15. Position relative to Golovin convergence

The diagnostic and statistical convergence software already exists. What has
not yet run is the controlled scientific resolution ensemble.

The remaining order is deliberately:

1. independently regenerate the same 4096-SD bundle on the same pinned Levante
   stack and require byte identity;
2. implement and verify the registered 250/500/1000-bin robustness calculation;
3. rebuild the controlled branch at its exact commit and run one short compiled
   Golovin member that consumes the frozen bundle;
4. audit runtime and Zarr storage for that member;
5. run the small collision-timestep screen;
6. begin the controlled \(N_\mathrm{SD}\) ladder and adaptive ensembles that
   constitute the actual Golovin convergence experiment.

Each new Slurm job remains a separate approval gate with a complete compute
request. The successful bundle job validates initialization identity and
provenance, not Golovin convergence.

## 16. Final pre-convergence tooling implemented

No new Slurm work was submitted while implementing this layer.

The repository now contains:

```text
scripts/levante/validate_controlled_bundle_replay.sbatch
scripts/levante/prepare_controlled_bundle_ladder.sbatch
scripts/levante/run_golovin_timestep_screen.sbatch
scripts/levante/analyze_golovin_timestep_screen.sbatch
scripts/analyze_golovin_timestep_screen.py
config/golovin_controlled_timestep_screen.yaml
experiments/golovin_controlled_timestep_screen_v1/
```

The matrix runner now accepts explicit controlled-bundle labels, while
operational rows must continue to use `not_applicable`. Controlled rows reject
an initialization seed and resolve their bundle below the persistent bundle
root.

The formal member diagnostics now calculate the registered fixed-bin Golovin
error on 250, 500 and 1000 logarithmic radius bins. The 500-bin value remains
the primary metric. The timestep decision requires:

- mean L1 change from 500 bins no larger than 0.005;
- the L1 timestep-equivalence decision to pass at all three bin counts;
- total below/above-range mass no larger than \(10^{-6}\);
- liquid-mass drift no larger than \(10^{-7}\);
- L1, signed relative \(M_0\) and signed relative \(M_6\) common-stream
  difference intervals inside their registered margins at every decision
  time.

During review, a proposed 4096-SD screen was corrected before submission.
The registered protocol requires the timestep screen at 16,384 SDs, where a
timestep bias is less likely to be hidden by coarse-resolution noise. The
final deterministic matrix contains:

```text
5 timesteps x 5 collision-stream labels = 25 members
N_SD = 16384
reference timestep = 0.1 s
```

Only five distinct 64-bit seed labels appear: each stream index is reused at
all five timesteps. This creates reproducible common-stream comparisons, not
paired event histories.

The resolution-bundle ladder is also explicit:

```text
512, 1024, 2048, 4096, 8192, 16384
```

The canonical 4096 bundle is verified and retained. Five missing native
bundles will be created non-destructively and made read-only before the
timestep screen and resolution ensemble.

Local validation currently includes:

```text
bash -n: all Levante entry points pass
Python compilation: pass
git diff --check: pass
targeted tests: 38 pass
```

The macOS base interpreter lacks `awkward`, so five diagnostic-module tests
cannot load there. The locked project environment in GitHub Actions and
Levante remains the authoritative full-suite gate.

The scientific and operational explanation is in
[`docs/implementation/golovin-preconvergence-gates.md`](../implementation/golovin-preconvergence-gates.md).
The next remote order is: replay, bundle ladder, exact-commit build, one
controlled compiled member and diagnostic, then the 16,384-SD timestep screen.
Only after all pass will the 120-member controlled resolution matrix be
materialized. No convergence conclusion exists yet.
