# Worklog: 2026-07-30

## 1. Controlled Golovin high-resolution result

The restart and dependent analysis for
`golovin_controlled_high_resolution_convergence_v1` completed successfully.

### Slurm outcome

| job | purpose | state | exit | elapsed | requested resources |
| --- | --- | --- | --- | ---: | --- |
| `26545309` | restartable model runner | `COMPLETED` | `0:0` | 3:33:45 | 4 tasks, 1 CPU/task, 3600 MiB, 4 h |
| `26545310` | serial convergence analysis | `COMPLETED` | `0:0` | 0:20:42 | 1 task, 1 CPU/task, 940 MiB, 45 min |

Slurm allocated four logical CPUs to the model and two to the analysis. The
analysis remained serial; the extra allocated logical CPU did not represent a
second analysis worker. Maximum batch RSS reported for the analysis was
564,504 KiB.

The restart validated and skipped all 265 members completed by the timed-out
job, reran the four quarantined 65,536-SD members, and ran the 131 cases that
had not started. The final audit found:

- 400 top-level member directories;
- 400 `status=completed` manifests;
- zero in-progress manifests;
- 100 independent collision members at each of 16,384, 32,768, 65,536 and
  131,072 SDs;
- 28,000,698,000 raw Zarr bytes, approximately 26.1 GiB;
- empty top-level stderr logs;
- worker stderr containing only Slurm step-creation notices;
- a completed model inventory and runner-completion record.

The compact analysis in HOME passed its complete `SHA256SUMS` audit. A
checksum-identical 4.7-MiB copy is retained in:

```text
results/golovin_controlled_high_resolution_convergence_v1/analysis_v1
```

Raw member data remain on Levante SCRATCH and are not copied into Git.

### Registered decision

The immutable decision is:

```text
status: no_resolution_accepted_in_initial_matrix
selected_max_superdroplets: null
```

This does **not** mean that the numerical solutions failed analytical
validation. Every tested resolution passed all registered analytical-accuracy,
ensemble-precision, initialization, conservation and diagnostic-range gates:

| \(N_\mathrm{SD}\) | analytical and precision pass |
| ---: | :---: |
| 16,384 | yes |
| 32,768 | yes |
| 65,536 | yes |
| 131,072 | yes |

The worst analytical limits across all decision times and registered bin
counts were:

| \(N_\mathrm{SD}\) | worst L1 upper 95% bound | worst absolute \(M_0\) interval edge | worst absolute \(M_6\) interval edge |
| ---: | ---: | ---: | ---: |
| 16,384 | 0.04369 | 0.0036 | 0.0241 |
| 32,768 | 0.03181 | 0.0028 | 0.0207 |
| 65,536 | 0.02127 | 0.0028 | 0.0116 |
| 131,072 | 0.01508 | 0.0021 | 0.0071 |

The registered limits are 0.05 for the L1 upper bound, 0.05 for the absolute
\(M_0\) relative-bias interval and 0.10 for the absolute \(M_6\)
relative-bias interval. Accuracy therefore improved materially with
resolution and remained comfortably inside the analytical margins.

Maximum absolute liquid-mass drift declined from \(5.115\times10^{-9}\) at
16,384 SDs to \(1.785\times10^{-9}\) at 131,072 SDs, well below the
\(10^{-7}\) gate. No member placed measurable liquid mass outside the
registered 1-5000 micrometre diagnostic range.

### Why no resolution was selected

The formal decision also requires practical equivalence for both adjacent
pairs in an \(N/2N/4N\) confirmation triple:

| adjacent pair | registered equivalence pass |
| --- | :---: |
| 16,384-32,768 | no |
| 32,768-65,536 | no |
| 65,536-131,072 | yes |

All \(M_0\) and \(M_6\) adjacent comparisons passed. The failures came only
from fixed-bin L1 intervals:

- 16,384-32,768 failed all six 1000-bin comparisons and the 500-bin
  comparisons at 3000 and 3600 s;
- 32,768-65,536 failed the 1000-bin comparisons at 3000 and 3600 s;
- 65,536-131,072 passed every metric, time and bin count.

The worst 32,768-65,536 comparison was the 1000-bin L1 difference at 3600 s:
the point estimate was 0.00816, within the 0.01 practical-equivalence margin,
but its 95% interval was [0.00592, 0.01333], which was not fully contained
inside the margin. Thus the remaining lower-pair failures are
uncertainty/containment failures rather than point estimates larger than the
registered L1 margin.

The highest pair provides positive evidence of a resolution plateau, but it
cannot by itself select 65,536 SDs: the pre-registered confirmation rule would
need a further 262,144-SD level. Conversely, additional independent members
at 32,768 and 65,536 could test whether the narrow lower-pair failures are
resolved by tighter intervals. Choosing between those continuations requires a
documented precision/cost assessment before further compute; no continuation
is authorized by this result.

## 2. Stricter M6 margins and explanatory reanalysis

The researcher amended both formal \(M_6\) margins from 10% to 5%:

- the analytical relative-bias 95% interval must lie inside \(\pm5\%\);
- the adjacent-resolution relative-change 95% interval must lie inside
  \(\pm5\%\).

The previous `analysis_v1` is immutable and still records the original
10%/10% definition. The amended configuration records analysis protocol
version 2. No CLEO member was rerun: all analyses below reuse the same 400
completed members.

### Three separate figures

The original dense summary was replaced by three figures with one purpose
each:

1. `analytical_accuracy.png` shows the largest L1 upper 95% bound over the
   registered 250/500/1000-bin grids, plus \(M_0\) and \(M_6\) bias, against
   \(N_\mathrm{SD}\) at all six decision times. Green regions show the
   registered analytical margins.
2. `adjacent_resolution_equivalence.png` shows what changes when
   \(N_\mathrm{SD}\) doubles. Its distribution panel shows the largest
   absolute L1 interval edge over the registered 250/500/1000-bin grids, so
   every formal bin-robustness failure is visible. Green regions show the
   registered adjacent-level margins.
3. `ensemble_size_stability.png` has one column per resolution and one row
   per primary property. At 3600 s it repeatedly samples distinct-member
   subsets of size 5, 10, 20, 40, 60 and 80 from the available 100 members;
   the 100-member point uses the complete pool exactly. The shaded region is
   the 2.5th-97.5th percentile range across 2000 random subsets. This is a
   descriptive stability diagnostic, not a new formal convergence gate and
   not an estimate based on additional independent model members.

The random-subset design is without replacement. An intermediate
`analysis_v2` used with-replacement bootstrap draws; it is retained for audit
but is not the preferred explanatory result because its nominal 100-member
draw can duplicate and omit members and therefore need not meet the actual
complete-pool estimate.

### Slurm and artifact audit

| job | role | state | elapsed | requested | maximum batch RSS |
| --- | --- | --- | ---: | --- | ---: |
| `26552498` | intermediate `analysis_v2` | `COMPLETED 0:0` | 6:07 | 1 CPU, 940 MiB, 1 h | 618,388 KiB |
| `26552563` | superseded plot correction | cancelled after method review | 0:19 | 1 CPU, 940 MiB, 1 h | not material |
| `26552584` | accepted subset-method `analysis_v3` | `COMPLETED 0:0` | 4:09 | 1 CPU, 940 MiB, 1 h | 668,920 KiB |
| `26552725` | canonical explanatory `analysis_v4` | `COMPLETED 0:0` | 1:34 | 1 CPU, 940 MiB, 1 h | 605,016 KiB |

Slurm allocated two logical CPUs to each completed shared-partition job, but
the analysis remained serial. All completed jobs had empty stderr and passed
their full `SHA256SUMS` audit. The checksum-identical local canonical record
is:

```text
results/golovin_controlled_high_resolution_convergence_v1/analysis_v4
```

The preferred `analysis_v4` preserves all three `analysis_v3` numerical CSVs
byte-for-byte. It changes only the two formal figures so that they display
the worst registered bin grid rather than one representative bin grid. Its
formal decision therefore remains:

```text
status: no_resolution_accepted_in_initial_matrix
selected_max_superdroplets: null
```

All four resolutions still pass their analytical and precision gates under
the stricter \(M_6\) rule. The worst \(M_6\) analytical interval edge is
2.410% (16,384 SDs at 3600 s), and the worst adjacent \(M_6\) interval edge is
3.651% (16,384 to 32,768 SDs at 3600 s). Both are inside 5%. The remaining
formal failures still come from fixed-bin distribution L1 comparisons, not
from \(M_0\) or \(M_6\).

### What the ensemble-size curves add

At 3600 s, the complete 100-member L1 estimates decrease monotonically with
resolution: 1.937%, 1.425%, 0.983% and 0.704% for 16,384 through 131,072 SDs.
The random-subset ranges narrow as distinct members are added at every
resolution and for every primary property.

\(M_0\) is least ensemble-demanding: its 100-member biases are within 0.13%
of zero and its subset range contracts rapidly. \(M_6\) is most
ensemble-demanding. At 16,384 SDs, the 95% subset half-width declines from
6.57 percentage points with five members to 2.98 with 20, 1.81 with 40 and
0.75 with 80; it becomes zero at 100 because there is only one complete
100-of-100 subset. At 131,072 SDs, the corresponding values are 2.56, 1.14,
0.71 and 0.29 percentage points. Thus increasing both resolution and ensemble
size improves stability, while high-order \(M_6\) remains visibly noisier
than \(M_0\).

## 3. All-row sample-size planning and lower-resolution context

The earlier controlled resolution screen is still available as a compact,
checksum-published record even though its audited raw SCRATCH tree was
deleted. It contains six resolutions (512, 1024, 2048, 4096, 8192 and 16,384
SDs) with 20 independent collision members per resolution. The current fresh
experiment contains 16,384, 32,768, 65,536 and 131,072 SDs with 100 members
per resolution.

The retained earlier analytical table is sufficient to plot the formal
ensemble estimates and intervals, but not to reconstruct new random-subset
ensemble-mean L1 distributions: the per-member fixed-bin archives were part
of the intentionally deleted raw tree. The new
`resolution_history_context.png` therefore plots the two published
experiments as separate series. It does not pool them, and it retains both
independent 16,384-SD results.

The history figure shows a consistent metric hierarchy:

- the worst registered L1 upper bound falls steeply with resolution, but the
  20-member screen remains above the 5% analytical region through its
  16,384-SD endpoint;
- most \(M_0\) point estimates are already close to zero at low resolution,
  although low-resolution 20-member intervals are broad;
- \(M_6\) is much noisier at low resolution, while the fresh 100-member
  16,384-131,072-SD results are tightly contained inside the new 5% region.

The two 16,384-SD results are not contradictory. They use independent
collision ensembles and different ensemble sizes. The 20-member result has a
much wider confidence interval; the fresh 100-member result passes the
analytical/precision gates.

### Planning projection

`analyze_golovin_followup.py` uses the current 100-member adjacent-L1 point
estimate and interval for every time/bin/pair. For planning member count
\(n\), it holds the point estimate fixed and contracts each side of the
observed interval by

\[
\sqrt{100/n}.
\]

This is the standard asymptotic planning approximation. It is not a new
bootstrap result, does not synthesize new members, and cannot guarantee that a
future ensemble mean will remain unchanged.

The worst-row projections are:

| resolution pair | currently passes all rows | projected members per resolution | limiting row |
| --- | --- | ---: | --- |
| 16,384-32,768 | no | 2,028 | 3,000 s, 1,000 bins |
| 32,768-65,536 | no | 788 | 3,600 s, 1,000 bins |
| 65,536-131,072 | yes | 100 | already passes |

These estimates show that a small blind expansion from 100 to 200 members is
unlikely to complete the strict all-time/all-bin L1 containment rule. They
also change the cost decision:

- establishing 16,384 as the smallest accepted level would require resolving
  both lower pairs and appears particularly expensive;
- testing 32,768 remains possible but projects to several hundred independent
  members at both 32,768 and 65,536;
- confirming 65,536 under the existing N/2N/4N rule instead requires a new
  262,144-SD level and an adequate 131,072-262,144 ensemble.

No additional model member should be submitted until this projected cost is
compared with a prospectively defined diminishing-returns alternative and
discussed with Clara.

### Levante audit

Analysis-only job `26553692` requested `bb1153/shared`, one node, one
task/CPU, 940 MiB and a 10-minute ceiling. It launched no CLEO model, read no
raw Zarr output, completed `0:0` in 12 seconds, had empty stderr and used
3,912 KiB maximum batch RSS; Slurm allocated two logical CPUs. All ten
published files pass their SHA-256 audit locally and remotely. The canonical
compact output is:

```text
results/golovin_controlled_high_resolution_convergence_v1/followup_v1
```

## 4. Literature-grounded practical convergence proposal

The strict all-time/all-bin adjacent-equivalence rule was reviewed against
Shima et al. (2009), Unterstrasser et al. (2017, 2020), Dziekan and
Pawlowska (2017), Schwenkel et al. (2018), Morrison et al. (2024),
Zmijewski et al. (2024), and the current CLEO numerical-method paper.
No paper uses the project's exact unsmoothed 250/500/1000-bin L1
confidence-containment rule. Published practice instead separates mean and
variance convergence and often identifies practical convergence from a small
final change compared with much larger changes at preceding resolution
doublings.

A proposed, not-yet-canonical ADR now defines:

- a hard analytical/correctness gate;
- a one-percentage-point minimum worthwhile change, equal to 20% of the
  existing 5% analytical-error budget;
- one-sided 95% bootstrap upper bounds on the absolute change for primary
  500-bin L1, \(M_0\) bias and \(M_6\) bias;
- N/2N/4N confirmation;
- a separate ensemble-prefix stability rule;
- 250/1000-bin calculations as mandatory robustness diagnostics rather than
  automatic fine-histogram vetoes.

The existing point estimates identify 32,768 SDs as the first plausible
candidate, but this is not yet a decision because the proposed upper bounds
and all-time ensemble-prefix rule have not been computed.

The scientific audit also found a small reference-definition mismatch. The
initializer conditions an exponential-in-volume DSD on 1--75 μm, whereas the
implemented closed-form Golovin reference is untruncated. The excluded number
fractions are \(3.51\times10^{-5}\) below 1 μm and
\(3.65\times10^{-7}\) above 75 μm; the initial conditioned/untruncated mass
L1 mismatch is \(4.13\times10^{-5}\) (0.00413%). This is far too small to
explain current 0.7%--1.9% primary L1 errors, but exact-solution language must
be qualified or a support sensitivity added before publication.

Full proposal and evidence:

```text
docs/decisions/0007-proposed-diminishing-returns-convergence.md
```

No configuration, analysis code, result artifact, Levante job, model member,
GitHub state or canonical decision was changed.

## 5. Practical-criterion implementation

After the researcher accepted the direction, the proposal was implemented as
a new analysis path without changing `analysis_v4` or the completed model
matrix.

The high-resolution configuration now records:

- primary 500-bin L1 and diagnostic 250/1000-bin sensitivity;
- a one-percentage-point minimum worthwhile improvement;
- independent one-sided 95% percentile-bootstrap upper bounds;
- member prefixes 20/40/60/80/100;
- an 80-to-100-member stability gate;
- two-successive-doubling confirmation.

`analyze_golovin_practical_convergence.py` calculates the analytical,
conservation and range gates at each prefix; independently bootstraps the
absolute change between adjacent resolutions; finds the smallest N/2N/4N
candidate; checks whether the final two member-prefix decisions and all
primary point estimates are stable; and reports alternative-bin results as
mandatory diagnostics rather than automatic vetoes. Its two figures show the
all-time diminishing-return bounds and how their worst values change as
members are added.

The new Levante wrapper is analysis-only, atomic and non-overwriting. It
checks the complete `analysis_v4` SHA-256 manifest before reading its combined
member table and reads the existing per-member fixed-bin archives from
SCRATCH. It requests one CPU, 940 MiB and a 20-minute ceiling; it contains no
`srun` or CLEO execution.

Local validation currently passes the 26 focused practical/resolution/
repository-contract tests, Ruff, Python compilation, Bash syntax and
`git diff --check`. No Levante job, new model member, raw Zarr output, GitHub
change or Long calculation has yet occurred.

### First practical calculation and visual correction

Analysis-only job `26554716` requested one CPU/940 MiB/20 minutes on
`bb1153/shared`, completed `0:0` in 4m02s, was allocated two logical CPUs,
used 438,292 KiB maximum batch RSS and had empty stderr. All output checksums
pass. The primary full-ensemble calculation selected no resolution: all hard
analytical gates and the 80-to-100-member stability gate pass, but every
adjacent pair fails at least one one-percentage-point diminishing-returns
upper bound. The limiting property is \(M_6\); 32,768--65,536 and
65,536--131,072 have sub-0.5-percentage-point final point changes but
one-sided upper bounds of 1.752 and 1.293 percentage points at 3600 s.

The numerical record is valid, but visual QA rejected the first
ensemble-prefix PNG because its long axis labels were clipped and crowded.
The active plotting source now uses explicit compact labels, font sizes and
subplot margins. Non-overwriting analysis-only job `26554881` then published
`practical_v2` from exact project commit `9f44646`. It requested one CPU,
940 MiB and 20 minutes on `bb1153/shared`, completed `0:0` in 5m32s, was
allocated two logical CPUs, used 438,144 KiB maximum batch RSS and had empty
stderr. All 13 output checksums pass remotely and locally. Every numerical CSV
is byte-identical to `practical_v1`; the decision JSON changes only the
provenance path used to address the same checksum-identical matrix. Both
corrected figures passed visual QA. `practical_v1` remains immutable.

The scientific conclusion is therefore not a generic failure to converge.
The hard analytical/correctness gates pass, and the complete practical
decision is stable at the final two member prefixes. No candidate is selected
because the one-sided confidence bound for late-time \(M_6\) still exceeds
one percentage point. The 16,384--32,768 point change itself reaches
1.672 percentage points at 3600 s, ruling out 16,384 as a practical candidate.
For the two upper pairs, the corresponding point changes are only 0.472 and
0.444 percentage points, so their remaining failures are uncertainty rather
than material-change failures.

A fixed-variance \(n^{-1/2}\) planning calculation gives approximately
590 balanced members per resolution for the limiting 32,768--65,536 row and
230 for 65,536--131,072. These values are only asymptotic planning estimates.
If Clara accepts the criterion, the scientifically efficient next experiment
is to add independent members to 32,768, 65,536 and 131,072 in measured
restartable blocks, rerunning the decision after each block. It is not to rerun
16,384 or immediately add 262,144.

### Immutable-result CI policy

GitHub quality runs `30523004015` and `30523913279` failed only because
repository-wide `ruff format --check .` attempted to reformat historical
Python source snapshots stored inside checksum-published `analysis_v2` and
`analysis_v3`. Those files are evidence, not active source: changing their
bytes would invalidate their SHA-256 manifests. After researcher approval,
`pyproject.toml` therefore excludes only `results/` from Ruff discovery.
The pre-commit end-of-file and trailing-whitespace fixers also exclude
`results/`, because even opening read-only evidence in write mode fails and
changing recorded stdout would invalidate provenance. Active `scripts/` and
`tests/` remain fully linted and formatted; published result trees remain
protected by their manifests. This changes no scientific calculation or
result.

Commit `cb9e23f` implements the narrow exclusion. The complete all-files
pre-commit suite, repository-wide Ruff checks and all 85 tests pass locally;
the controlled-bundle and both practical-analysis checksum manifests still
pass. GitHub Actions quality run `30525768243` then passed in its clean Linux
environment. The earlier failed runs remain useful evidence of why active
source and immutable result snapshots require different quality controls.

## Pilot-informed extension planning

Clara gave the researcher freedom to explore provided that decisions remain
literature-grounded and defensible for a possible paper. This authorizes
method development, not an unrecorded change to the convergence criterion or
unbounded compute.

ADR 0008 therefore separates three activities:

1. use the existing 100-member outcomes to estimate per-resolution,
   per-time and per-metric variance coefficients;
2. project a fixed final member allocation before inspecting new formal
   outcomes; and
3. reserve any repeated interim stopping for a future prospectively
   alpha-adjusted or time-uniform method.

The literature supports this separation. SDM studies use ensemble sizes that
depend on the diagnostic and distinguish ensemble-mean convergence from
stochastic variance. Simulation-output literature sizes a fixed calculation
to a preassigned interval width. Cost-allocation literature motivates placing
samples where variance reduction per cost is greatest. Confidence-sequence
literature explains why ordinary fixed-sample 95% intervals cannot simply be
rechecked until one happens to pass.

The new planner:

- uses only 32,768, 65,536 and 131,072 SDs, because 16,384 is already ruled
  out by its observed late-\(M_6\) point change;
- bootstraps all primary metrics at all six decision times;
- fits the transparent planning approximation
  \(\operatorname{Var}(\hat q_n)=a/n\);
- records its discrepancy from the current percentile-bootstrap bound;
- searches both balanced and measured-cost-aware fixed allocations;
- publishes the limiting metric/time for each design; and
- sets `new_model_compute_authorized=false`.

Four focused tests prove monotone precision with member count, refusal of
unprotected optional stopping, impossibility of rescuing a point change above
the scientific margin by adding members, and a passing result from the
cost-aware allocation search. The project-owned Slurm wrapper is
non-overwriting and checksum-validates both `analysis_v4` and `practical_v2`.
It requests one serial CPU, 940 MiB and 20 minutes on `shared`, contains no
`srun`, and launches no CLEO executable.
