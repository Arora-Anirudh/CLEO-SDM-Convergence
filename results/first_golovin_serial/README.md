# First Golovin serial diagnostic record

This directory contains the compact, checksum-verified diagnostic record for
the project-owned `collisions0d` run `first_golovin_serial`.

- model job: `26516775`;
- final diagnostic job: `26517314`;
- model configuration: one MPI rank, one model thread, 4,096 superdroplets,
  Golovin collision-coalescence, initialization seed 12,345;
- raw Zarr location on Levante:
  `/scratch/b/b383673/SDM/CLEO-SDM-Convergence/runs/first_golovin_serial/output/collisions0d_solution.zarr`;
- compact products: `analysis_v1/`.

The numerical/analytical mass-distribution figure is generated by CLEO's
pinned `cleopy` and `plotcleo.shima2009fig` tools. The bulk figure and CSV add
project-owned number concentration, liquid water, conservation, maximum-radius,
threshold-mass and normalized Golovin L1 diagnostics.

The final diagnostic job completed with empty stderr and verified `SHA256SUMS`
after the atomic staging-to-final rename. Earlier failed/superseded diagnostic
directories are retained on Levante for audit but are not part of the canonical
repository result.

This is a single-run validation product, not convergence evidence.
